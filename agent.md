# Agent Instructions

- Read `docs/specifications.md` for functional and non-functional requirements.
- Read `docs/architecture.md` first for system structure and component boundaries.

- Project: `user_registration_api` (Dailymotion test project).
- Stack: FastAPI service.
- Local run: `docker compose up --build`.
- API base URL: `http://localhost:8000`.
- Docs endpoints: `/docs` (Swagger), `/redoc` (ReDoc), `/heartbeat` (health check).
- Tests: `docker compose run --rm test`.
